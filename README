Simple Kundera/Cassandra test example

* Issue #90
* Issue #91

- requires local instance of cassandra (default ports etc)

Build core : cd core-package ; mvn install
Then build app : cd app-package ; mvn package

App is empty, all code is in a UT.
